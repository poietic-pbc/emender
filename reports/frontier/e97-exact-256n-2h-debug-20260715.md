# Exact E97 256-node two-hour debug validation

## Launch

Slurm job `5000354` was submitted exactly once at 2026-07-15 02:06 EDT from
the immutable attested working tree used by production job `4980157`.
Production job `4980157` was queried read-only and remains unmodified.

The fail-closed structured comparison in
`build/e97-256/exact-debug-256n-2h-20260715/structured-command-diff.json`
records exactly two semantic differences from the production SubmitLine:
walltime `12:00:00` to `02:00:00` and QoS `normal` to `debug`. Nodes, ranks,
partition, account, launcher, environment, seed, training caps, and output
base are identical.

The retained source manifest, launcher SHA256
`106a4dde6b966b0af66a1ac92ea0f459c7a435f81f6e322d92e08f30a2cfad30`,
and pinned step-1525000 seed SHA256
`1da27d2e09bc6c6f5ffc30e3e4476df1cebd807267431c8524de1a5b0dc5bca9`
all passed before submission. The promotion suite passed 67/67 tests. Two
additional launcher tests could not import their target under the login
node's Python 3.6 because that source requires future annotations; this was
an interpreter mismatch, not a test assertion failure.

## Current status

At initial capture job `5000354` was `PENDING (Priority)` in `batch/debug`,
requesting exactly 256 nodes for two hours. Its scheduler estimate was
2026-07-15 03:30 EDT. Terminal accounting and run acceptance evidence will
be appended after scheduler-controlled completion.

## Durability audit and corrected replacement

Job `5000354` was cancelled while still pending at 2026-07-15 02:22:11 EDT
because the original attested launcher did not publish an explicit immutable
last-valid continuation handoff on scheduler exit. It consumed no allocation.
Production job `4980157` remained read-only and pending.

The minimum scheduler-exit shim is
`build/e97-256/exact-debug-256n-2h-20260715/durable-debug-wrapper.sbatch`.
It executes the original attested launcher and source tree unchanged, requests
a batch-shell TERM signal five minutes before the scheduler limit, preserves
the normal atomic every-generation checkpoints, and atomically publishes
`RUN_ROOT/continuation/last-valid.json`. That handoff names the last completed
immutable `.pt`, its SHA256 and size, generation/step when present, and the
source run-local `latest.json`. The next launch resolves it with
`select-continuation-seed.py`, which fails closed on missing files or checksum
and size drift. An interrupted in-flight generation is never selected.

The sole actual replacement submission created job `5000436` at 2026-07-15
02:31:50 EDT. It is 256 nodes, 2048 tasks, `batch/debug`, and `02:00:00`; its
initial state is `PENDING (Priority)` with `StartTime=Unknown`. The persistent
run root will be:

```
/lustre/orion/bif148/proj-shared/emender/frontier_runs/debug/async_quorum_b4k40_ladder_20260709/20260715/E97_1.3B_step1065000_async_quorum_b4k40_ladder_256n/5000436-<UTC-launch-stamp>
```

The `step1065000` directory component is a stale cosmetic variant label only.
The submitted checkpoint remains the approved step-1525000 file with SHA256
`1da27d2e09bc6c6f5ffc30e3e4476df1cebd807267431c8524de1a5b0dc5bca9`.
The continuation handoff will be at
`RUN_ROOT/continuation/last-valid.json`; normal run-local restart pointers
remain `RUN_ROOT/async_run/latest.json` and `RUN_ROOT/async_run/latest.pt`.

The structured replacement diff records exactly QoS, walltime, and the
scheduler-exit wrapper path. Training exports and the original launcher hash
are unchanged. Two helper executions stopped locally before invoking `sbatch`
(a formatted cancelled-state guard and Python 3.6 `text=` incompatibility);
Slurm accounting showed no duplicate job. The single actual `sbatch` call
returned `5000436`.
