# Exact E97 256-node two-hour debug validation

## Launch

Slurm job `5000354` was submitted exactly once at 2026-07-15 02:06 EDT from
the immutable attested working tree used by production job `4980157`.
Production job `4980157` was queried read-only and remains unmodified.

The fail-closed structured comparison in
`build/e97-256/exact-debug-256n-2h-20260715/structured-command-diff.json`
records exactly two semantic differences from the production SubmitLine:
walltime `12:00:00` to `02:00:00` and QoS `normal` to `debug`. Nodes, ranks,
partition, account, launcher, environment, seed, training caps, and output
base are identical.

The retained source manifest, launcher SHA256
`106a4dde6b966b0af66a1ac92ea0f459c7a435f81f6e322d92e08f30a2cfad30`,
and pinned step-1525000 seed SHA256
`1da27d2e09bc6c6f5ffc30e3e4476df1cebd807267431c8524de1a5b0dc5bca9`
all passed before submission. The promotion suite passed 67/67 tests. Two
additional launcher tests could not import their target under the login
node's Python 3.6 because that source requires future annotations; this was
an interpreter mismatch, not a test assertion failure.

## Current status

At initial capture job `5000354` was `PENDING (Priority)` in `batch/debug`,
requesting exactly 256 nodes for two hours. Its scheduler estimate was
2026-07-15 03:30 EDT. Terminal accounting and run acceptance evidence will
be appended after scheduler-controlled completion.

## Durability audit and corrected replacement

Job `5000354` was cancelled while still pending at 2026-07-15 02:22:11 EDT
because the original attested launcher did not publish an explicit immutable
last-valid continuation handoff on scheduler exit. It consumed no allocation.
Production job `4980157` remained read-only and pending.

The minimum scheduler-exit shim is
`build/e97-256/exact-debug-256n-2h-20260715/durable-debug-wrapper.sbatch`.
It executes the original attested launcher and source tree unchanged, requests
a batch-shell TERM signal five minutes before the scheduler limit, preserves
the normal atomic every-generation checkpoints, and atomically publishes
`RUN_ROOT/continuation/last-valid.json`. That handoff names the last completed
immutable `.pt`, its SHA256 and size, generation/step when present, and the
source run-local `latest.json`. The next launch resolves it with
`select-continuation-seed.py`, which fails closed on missing files or checksum
and size drift. An interrupted in-flight generation is never selected.

The sole actual replacement submission created job `5000436` at 2026-07-15
02:31:50 EDT. It is 256 nodes, 2048 tasks, `batch/debug`, and `02:00:00`; its
initial state is `PENDING (Priority)` with `StartTime=Unknown`. The persistent
run root will be:

```
/lustre/orion/bif148/proj-shared/emender/frontier_runs/debug/async_quorum_b4k40_ladder_20260709/20260715/E97_1.3B_step1065000_async_quorum_b4k40_ladder_256n/5000436-<UTC-launch-stamp>
```

The `step1065000` directory component is a stale cosmetic variant label only.
The submitted checkpoint remains the approved step-1525000 file with SHA256
`1da27d2e09bc6c6f5ffc30e3e4476df1cebd807267431c8524de1a5b0dc5bca9`.
The continuation handoff will be at
`RUN_ROOT/continuation/last-valid.json`; normal run-local restart pointers
remain `RUN_ROOT/async_run/latest.json` and `RUN_ROOT/async_run/latest.pt`.

The structured replacement diff records exactly QoS, walltime, and the
scheduler-exit wrapper path. Training exports and the original launcher hash
are unchanged. Two helper executions stopped locally before invoking `sbatch`
(a formatted cancelled-state guard and Python 3.6 `text=` incompatibility);
Slurm accounting showed no duplicate job. The single actual `sbatch` call
returned `5000436`.

## Runtime monitoring

Replacement job `5000436` entered `RUNNING` at 2026-07-15 02:45:14 EDT on
exactly 256 nodes, 2,048 tasks, and 2,048 GPUs. Its scheduler end time is
2026-07-15 04:45:14 EDT. The persistent run root resolved to
`.../5000436-20260715T064518Z`. During the first four minutes, its persistent
artifact count advanced through 7, 39, 71, 399, and 2,103 files. The launcher
reported both `trainpy_launched_ranks=2048` and `async_expected_ranks=2048`,
and its rendered `srun` retained the approved step-1525000 checkpoint. No
fail-fast condition was observed in this startup window. Production job
`4980157` remained read-only and pending.

At 2026-07-15 03:12 EDT, the run passed its minimum progress gate. The rank
ledger contained exactly 2,048 unique ranks on 256 unique nodes. Generations
0 through 5 were finalized and authoritative, and every merge recorded all
2,048 participating workers with zero failed, invalid, missing, late, stale,
rejected, or timed-out updates. Six distinct 15,439,252,298-byte checkpoints
were finalized. The newest was step 1,525,240 (240 steps beyond the approved
seed) with finite loss 2.4313. Generation duration remained 90.1--92.8
seconds and merge duration 4.75--4.86 seconds. No fail-fast signature was
found. The detailed milestone is preserved in
`replacement-five-generation-gate-20260715T0712Z.txt`; monitoring continues
at ten-minute cadence until terminal scheduler accounting and independent
reload/handoff validation are available.

## Authorized early cancellation and terminal disposition

At the user's explicit direction, debug job `5000436` was cancelled without a
replacement. Slurm recorded `CANCELLED by 19032` at 03:27:57 EDT after 42:43
elapsed; the 2,048-rank step ended by signal 15. This task therefore remains
incomplete: it did not run near the two-hour scheduler limit, and an independent
model/optimizer reload was not completed before cancellation.

The run itself was advancing normally rather than stalled. Generations 0 through
9 finalized before cancellation, producing ten immutable checkpoints and reaching
step 1,525,400 (400 steps beyond the approved seed). The final completed checkpoint
is:

```
/lustre/orion/bif148/proj-shared/emender/frontier_runs/debug/async_quorum_b4k40_ladder_20260709/20260715/E97_1.3B_step1065000_async_quorum_b4k40_ladder_256n/5000436-20260715T064518Z/async_run/checkpoints/emender_E97_100m_20260715/checkpoint_step_1525400_loss_2.4184.pt
```

It is 15,439,252,298 bytes with SHA256
`ee9d69d9c3efd5696042b30ad1ad57236d5035876bae5ce2e9cc2010e5017fd3`.
The TERM-aware wrapper published immutable handoff
`continuation/last-valid-20260715T072807Z.json` and atomically pointed
`continuation/last-valid.json` to it. The fail-closed continuation selector
re-read the full checkpoint and passed its path, size, and SHA256 checks.

The terminal classification is scheduler/user cancellation, not a deterministic
code/config failure, scale transport failure, OOM, node/rank loss, or non-finite
loss. No additional 256-node job was submitted.

Production job `4980157` was never mutated by this task. A final read-only audit
found that it had independently changed to `CANCELLED` at 03:28:54 EDT, 57 seconds
after the debug job ended; this worker issued no cancellation or other mutation
against it. Exact accounting and handoff details are preserved in
`replacement-terminal-20260715T0728Z.txt`.

At 2026-07-15 03:17 EDT, Slurm still reported job `5000436` `RUNNING` with
`Reason=None`, 31:52 elapsed, and the unchanged scheduler end time of 04:45:14
EDT.  Allocation remained exactly 256 nodes, 2,048 tasks, and 2,048 GPUs.
`sstat` for step `.0` reported `MaxRSS=71484836K` on `frontier00060`; this is
consistent with the earlier 71,470,324 KiB observation rather than an upward
memory trend.  No job mutation was performed.  The six-generation gate above
remains the latest fully audited application milestone for this observation;
monitoring continues at the required ten-minute cadence.
