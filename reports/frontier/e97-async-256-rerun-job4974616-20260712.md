# Exact canonical E97 256-node smoke rerun: job 4974616

Date: 2026-07-12

WG task: `rerun-exact-proven-256`

Final status: **successful exact-topology smoke**. This is promotion evidence
for the fingerprint named below, but it is not a production submission or a
production-submission approval.

## Pre-submission proof

Before submission, the canonical renderer produced both profiles and the
fail-closed promotion checker returned:

```json
{"allowed_differences":{"partition":["batch","batch"],"qos":["debug","normal"],"walltime":["00:20:00","12:00:00"]},"fingerprint":"1704a835d62893ffc86e0d4eebde9a6b368b045cee509ce41c4a3176547ee3ee","ok":true,"training_stop_budget":{"generations":1,"local_steps":40,"steps":40,"timeout_s":1200,"walltime_remaining_s":1200}}
```

The rendered launcher SHA-256 was
`106a4dde6b966b0af66a1ac92ea0f459c7a435f81f6e322d92e08f30a2cfad30`,
matching the job-4962400 golden manifest and the launch record. The normalized
smoke and production fingerprints were identical:
`1704a835d62893ffc86e0d4eebde9a6b368b045cee509ce41c4a3176547ee3ee`.
The launcher-scoped test suite passed, 34 tests out of 34.

The smoke profile resolves `-N 256`, `-t 00:20:00`, `-p batch`, `-q debug`,
`ASYNC_TRAINPY_RANKS=2048`, `ASYNC_EXPECTED_RANKS=2048`, and
`ASYNC_GLOBAL_QUORUM=2048`. The paired production rendering changes only the
allowlisted walltime and QoS values. No production submission command was run.

## Concrete submission

The exact authority invoked was:

```bash
scripts/frontier/submit_e97_async_256_smoke.sh
```

It reran both renderers and the parity gate, then invoked the rendered smoke
`sbatch_argv`. Slurm returned job ID **4974616** with submission exit status
zero. `scontrol show job 4974616` independently reported:

- `JobState=PENDING` (initially `Resources`, subsequently `Priority`)
- `Account=bif148`, `Partition=batch`, `QOS=debug`
- `TimeLimit=00:20:00`
- `NumNodes=256-256`, `NumTasks=2048`, `TresPerTask=cpu=7,gres/gpu=1`
- command `scripts/frontier/trainpy_async_quorum_2n_smoke.sbatch`
- stdout `logs/frontier/trainpy_async_quorum/async-b4k40-ladder-256n-4974616.out`
- stderr `logs/frontier/trainpy_async_quorum/async-b4k40-ladder-256n-4974616.err`

At 08:10 EDT, `squeue --start` estimated a 09:26 EDT start. The allocation
actually started at 08:55:29 EDT and reached terminal state at 09:03:59 EDT.

## Terminal validation

`sacct -j 4974616 --starttime 2026-07-12` reports the allocation and every
recorded step `COMPLETED 0:0`. The allocation used 256 nodes and elapsed
`00:08:30`; its 2048-rank `srun` step elapsed `00:08:04`.

The run-local machine summary reports `Validation: pass`, payload exit status
zero, and exactly `2048 / 2048` rank starts. Its manifest reports
`rank_start_count=2048`, `expected_launched_ranks=2048`,
`accepted_updates=2048`, zero timed-out updates, and no validation errors. The
global generation metrics independently report:

- requested, participating, quorum, and accepted workers/updates: 2048;
- failed, invalid, late, missing, rejected, stale, and timed-out updates: zero;
- finite loss and loss-100: `2.52083`;
- one advanced DiLoCo generation (`generation=0`, `quorum_status=advanced`);
- merge duration `5.304643992334604` seconds;
- compiled Cray MPICH `MPI_Reduce`, world size 2048, with all launched ranks
  accepted and `5,506,770,496` aggregate update bytes;
- `671,416,320` accepted training tokens.

The global publisher finalized recovery record
`async_run/recovery_checkpoints/gen_000000/walltime_finalization.json` with
`authoritative=true` and `finalized=true`. It published the train.py-compatible
checkpoint
`async_run/checkpoints/emender_E97_100m_20260712/checkpoint_step_1065040_loss_2.5208.pt`
at 15,439,252,298 bytes. The canonical checkpoint publisher writes a temporary
checkpoint, reloads it with `torch.load(..., map_location="cpu")`, validates
that model and optimizer state exist and that the reloaded step matches, and
only then atomically renames it and advances the run-local `latest.pt`.
Therefore the existence of this final published path is downstream of the
successful reload check, not merely a save attempt. `latest.json` names the
same checkpoint and was published only inside this debug run.

## External-pointer guard and promotion boundary

The submission export contains no production chain pointer or updater. Every
latest, manifest, recovery, export, and model-checkpoint path emitted by job
4974616 is below its run-local debug root. The external production guard remains
the symlink
`frontier_runs/chains/E97_1.3B_step489920_b4_k80_64n_hier_g4_bucket64m_avg/latest.pt`
to the step-489920 checkpoint; its modification time `1783092774` predates this
job, whose outputs have modification time `1783861432`. The corresponding
production `last.pt` remains absent. The read-only seed `latest.pt` still
targets `checkpoint_step_1065000_loss_2.5386.pt` and also predates this run.

The exact successful normalized fingerprint is
`1704a835d62893ffc86e0d4eebde9a6b368b045cee509ce41c4a3176547ee3ee`;
the byte-for-byte launcher SHA-256 is
`106a4dde6b966b0af66a1ac92ea0f459c7a435f81f6e322d92e08f30a2cfad30`.
No production renderer submission gate (`--submit`) and no production `sbatch`
command was invoked by this task. Any later production launch must separately
consume this exact fingerprint and satisfy its approval gate.

## Evidence locations

- Slurm stdout/stderr:
  `logs/frontier/trainpy_async_quorum/async-b4k40-ladder-256n-4974616.{out,err}`
- run summary: `$RUN_ROOT/summaries/summary.md`
- machine manifest: `$RUN_ROOT/artifacts/manifest.json`
- metrics: `$RUN_ROOT/artifacts/metrics.json`
- exact command and environment: `$RUN_ROOT/artifacts/{command.txt,env.txt}`
- rank starts: `$RUN_ROOT/artifacts/rank-start.tsv`
- run root:
  `/lustre/orion/bif148/proj-shared/emender/frontier_runs/debug/async_quorum_b4k40_ladder_20260709/20260712/E97_1.3B_step1065000_async_quorum_b4k40_ladder_256n/4974616-20260712T125530Z`
