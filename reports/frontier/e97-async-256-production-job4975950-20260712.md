# E97 pinned-step production job 4975950

Task: `promote-pinned-1525000-e97`

## Submission

The retained successful smoke attestation binds launch payload commit
`57884f17138843359fcdf164e178e329f6cb6f71`. The reviewed gate and submission
code was pushed at `46d8ebfdf963ed709abbd09fe1ab454bee969e36` before submission.

The exact canonical command was invoked once:

```text
python3 scripts/frontier/check_e97_async_promotion.py --smoke build/e97-256/smoke --production build/e97-256/production --policy configs/frontier/e97_async_256_parity_policy.json --require-promotion --submit --approval build/e97-256/production/reviewed-approval.json
```

It exited 0 and returned production job ID `4975950`.

The normalized fingerprint is
`623f8cf34e88b614dabfcf5b130f7a6cb1ca702efe48b44149d5765cc3bf850a`.
The immutable seed is step 1525000, loss 2.4378, 99.9424B tokens,
7719679924 bytes, SHA256
`1da27d2e09bc6c6f5ffc30e3e4476df1cebd807267431c8524de1a5b0dc5bca9`.

## Parity

The fail-closed `--require-promotion` preflight returned `ok=true`. Its only
structured differences were walltime `00:20:00` to `12:00:00` and QoS
`debug` to `normal`; partition remained `batch`. The training stop budget,
launcher bytes/body, topology, argv outside the allowlist, environment,
canonical inputs and hashes remained identical.

## Initial scheduler state

At the first observation job `4975950` was `PENDING (Priority)`, requesting
256 nodes, 2048 tasks, 8 tasks/GPUs per node, partition `batch`, QoS `normal`,
account `bif148`, and time limit `12:00:00`. Slurm estimated a start at
`2026-07-12T23:20:00-04:00`. No stdout/stderr existed yet because the
allocation had not started. Exact initial `squeue`, `scontrol`, and `sacct`
captures and retained log paths are under `build/e97-256/production/evidence`.

This is initial submission evidence, not a claim of training health or terminal
success. Monitoring must continue through allocation startup, pinned-seed
hash/load verification, 2048-rank startup, finite loss, accepted updates, an
expected DiLoCo merge, and checkpoint/publication health without changing the
stable seed pointer.

## Queued monitoring checkpoint at 20:15 UTC

The bounded follow-up observation still found job `4975950` `PENDING` for
`Priority`. `scontrol` retained the exact 256-node, 2,048-task,
`batch`/`normal`, `12:00:00` request and estimated a start at
`2026-07-12T23:20:00-04:00`. `sacct -X` also reported `PENDING`; no allocation
or job steps existed. The retained stdout and stderr paths did not exist yet,
which is expected before startup. Consequently, seed verification, finite
loss, accepted updates, the DiLoCo merge, and checkpoint/publication behavior
remain pending rather than inferred.

The eight predecessor launch/monitor paths named in the task remain paused and
tagged `superseded-by-1525000`; this observation did not resume or mutate them.

## Terminal validation

Job `4975950` started at `2026-07-12T23:09:16` and ended at
`2026-07-12T23:18:27`. Slurm records the allocation and batch step as
`COMPLETED 0:0`; step `4975950.0` ran on 256 nodes with 2,048 tasks and also
completed `0:0`. The requested limit remained `12:00:00`, partition `batch`,
and QoS `normal`. The retained stop budget intentionally remained identical to
the successful smoke (one generation, 40 local steps, 1,200-second timeout),
so the workload completed before its production allocation limit.

The rank-start record contains exactly 2,048 unique ranks, contiguous from 0
through 2,047, each reporting world size 2,048. The run summary reports
`Validation: pass`, 2,048 accepted updates, zero failed, invalid, missing,
stale, rejected, late, or timed-out updates, and 671,416,320 accepted tokens.
Loss was finite at `2.4854`. Generation 0 advanced the full 2,048-rank quorum;
the compiled MPICH reduction completed and the DiLoCo merge took
`5.2828857228159904` seconds.

The exact retained seed was independently rechecked after the job: size
7,719,679,924 bytes, SHA256
`1da27d2e09bc6c6f5ffc30e3e4476df1cebd807267431c8524de1a5b0dc5bca9`,
and `torch.load` returned a checkpoint dictionary at step 1,525,000. The full
retained metadata checksum manifest still passes, including
`latest_emender_E97_1.3B.json` and `latest_symlink_target.txt`; the stable seed
pointer was not modified.

Generation publication produced the generation manifest, initial recovery and
export records, walltime-finalization record, run-local `latest.json`, and the
final checkpoint `checkpoint_step_1525040_loss_2.4854.pt`. The 15,439,252,298
byte final checkpoint was independently loaded successfully and reports step
1,525,040 and loss `2.4854`. Structured terminal evidence, the terminal Slurm
capture, and independent seed/final-checkpoint checks are retained in
`build/e97-256/production/evidence`.
