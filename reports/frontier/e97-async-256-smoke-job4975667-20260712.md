# Exact pinned E97 256-node smoke — job 4975667

Date: 2026-07-12

## Launch authority

The tracked worktree was clean after `git fetch origin`, and launch-time
`HEAD` equaled `origin/main` at
`57884f17138843359fcdf164e178e329f6cb6f71`. The only submission command was:

```text
scripts/frontier/submit_e97_async_256_smoke.sh
```

It exited zero and returned Slurm job `4975667`. No production submission was
made. The launcher rendered both profiles with normalized fingerprint
`623f8cf34e88b614dabfcf5b130f7a6cb1ca702efe48b44149d5765cc3bf850a`.
The parity gate returned `ok=true`; the only profile differences were walltime
`00:20:00` versus `12:00:00` and QoS `debug` versus `normal`. Partition was
`batch` for both, and the training stop budget was identical.

## Immutable seed verification

The launch resolved the immutable object directly, with no
`latest_emender_E97_1.3B.json` in the launch-time path:

`s3://spinozans/emender/e97-diloco/emender_E97_1.3B_20260709_084606/step_1525000/checkpoint_step_1525000_loss_2.4378.pt`

Pre-submit `stat` and full `sha256sum` returned 7,719,679,924 bytes and
`1da27d2e09bc6c6f5ffc30e3e4476df1cebd807267431c8524de1a5b0dc5bca9`.
The canonical Frontier PyTorch runtime successfully loaded a dictionary with
`checkpoint_metadata`, `loss`, `model_state_dict`, `optimizer_state_dict`, and
`step`. The pinned metadata are step 1,525,000, loss 2.4378, and 99.9424B
tokens. After the run, `sha256sum -c metadata_files.sha256` still passed for
the complete retained seed bundle, including its stable latest manifest and
symlink-target record; the external seed pointer was unchanged.

The earlier login-shell `python3` attempt failed only because that intake
shell did not expose PyTorch. It occurred before submission, changed no file,
and did not alter the canonical launcher or submitted environment. The actual
job used Python 3.12.13 and PyTorch 2.10.0+rocm7.1 from the configured Frontier
environment.

## Terminal evidence

Slurm accounting reports the allocation and batch step `COMPLETED 0:0` on 256
nodes. Step `4975667.0` reports `COMPLETED 0:0`, 256 nodes, and exactly 2,048
tasks. `rank-start.tsv` contains 2,048 unique ranks spanning exactly 0 through
2047.

The retained run manifest reports validation `pass`, exit status 0, 2,048
accepted updates, zero timed-out updates, and no validation errors. Generation
0 advanced at quorum 2,048/2,048. Its finite aggregate loss was 2.4854; the
DiLoCo merge completed in 5.4451 seconds with zero failed, invalid, stale,
missing, rejected, or timed-out updates. The finalized run checkpoint
`checkpoint_step_1525040_loss_2.4854.pt` is 15,439,252,298 bytes. An independent
post-run `torch.load(..., mmap=True)` succeeded and returned step 1,525,040,
loss 2.4854, and the expected model/optimizer keys.

## Retained evidence

The canonical evidence bundle is `build/e97-256/smoke/`. It includes
`launch-inputs.json`, `golden-manifest.json`, `rendered.sbatch`,
`fingerprint.sha256`, `promotion.json`, parity/submission/monitoring records,
terminal `sacct` and `scontrol`, complete Slurm stdout/stderr, rank starts,
run manifest/metrics/summary/latest/generation manifest, seed checksum results,
and final-checkpoint reload output. The durable training artifacts remain under:

`/lustre/orion/bif148/proj-shared/emender/frontier_runs/debug/async_quorum_b4k40_ladder_20260709/20260712/E97_1.3B_step1065000_async_quorum_b4k40_ladder_256n/4975667-20260712T171513Z`
