Runtime-required marker for watchdog-b4-k80 by agent-509.

This file exists only because the batch-worker runtime required a disk write
and git commit before completion. The watchdog observation itself was recorded
with `wg log watchdog-b4-k80`; no code, wrappers, docs, reports, scheduler
state, or run artifacts were edited.
